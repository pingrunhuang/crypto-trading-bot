[project]
name = "crypto-trading-bot"
version = "0.1.0"
description = ""
authors = [
    {name = "Huang Runping", email = "pingrunhuang@gmail.com"},
]
dependencies = [
    "requests>=2.28.1",
    "ccxt>=2.1.33",
    "fastapi>=0.86.0",
    "websocket>=0.2.1",
]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-pep517>=1.0.0"]
build-backend = "pdm.pep517.api"

[tool.pdm]
